# API Contracts — multimediaApp

N/A por ahora — app de escritorio sin API HTTP. Cuando el port use comandos Tauri (`invoke`) entre React y Rust, documentar acá cada comando con su payload y retorno.

## Comandos Tauri (a definir en ADR-001 + diseño)
| Comando | Payload | Retorno | Estado |
|---------|---------|---------|--------|
